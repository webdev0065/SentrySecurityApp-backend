import { Name, Value } from "../generalTypes.js";

//#region src/operations/triggers/shared.d.ts
interface TriggerOptions {
  when?: 'BEFORE' | 'AFTER' | 'INSTEAD OF';
  operation: string | string[];
  constraint?: boolean;
  function?: Name;
  functionParams?: Value[];
  level?: 'STATEMENT' | 'ROW';
  condition?: string;
  deferrable?: boolean;
  deferred?: boolean;
}
//#endregion
export { TriggerOptions };