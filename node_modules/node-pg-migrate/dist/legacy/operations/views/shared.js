//#region src/operations/views/shared.ts
function viewOptionStr(options) {
	return (key) => {
		return `${key}${options[key] === true ? "" : ` = ${options[key]}`}`;
	};
}
//#endregion
export { viewOptionStr };
