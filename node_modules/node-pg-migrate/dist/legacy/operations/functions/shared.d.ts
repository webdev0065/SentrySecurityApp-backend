import { LiteralUnion, Value } from "../generalTypes.js";

//#region src/operations/functions/shared.d.ts
interface FunctionParamType {
  mode?: 'IN' | 'OUT' | 'INOUT' | 'VARIADIC';
  name?: string;
  type: string;
  default?: Value;
}
type FunctionParam = string | FunctionParamType;
interface FunctionOptions {
  returns?: string;
  language: string;
  replace?: boolean;
  window?: boolean;
  behavior?: 'IMMUTABLE' | 'STABLE' | 'VOLATILE';
  security?: 'INVOKER' | 'DEFINER';
  onNull?: boolean;
  parallel?: 'UNSAFE' | 'RESTRICTED' | 'SAFE';
  set?: Array<{
    configurationParameter: string;
    value: LiteralUnion<'FROM CURRENT'>;
  }>;
}
//#endregion
export { FunctionOptions, FunctionParam, FunctionParamType };